name: journey-live-player-journey
on:
  push:
    branches: [ "arena/**" ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  journey:
    runs-on: ubuntu-latest
    timeout-minutes: 55
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Build plugin from this HEAD
        run: bash ci/build-and-test.sh
      - name: Fetch Paper 1.21.11 (paperclip bootstrap)
        run: |
          set -euo pipefail
          mkdir -p journey-server
          LIST_URL='https://fill.papermc.io/v3/projects/paper/versions/1.21.11/builds'
          curl -sSL -o /tmp/builds.json "$LIST_URL"
          INFO=$(python3 ci/resolve-paper.py /tmp/builds.json)
          URL=$(printf '%s' "$INFO" | python3 -c 'import json,sys; print(json.load(sys.stdin)["url"])')
          NAME=$(printf '%s' "$INFO" | python3 -c 'import json,sys; print(json.load(sys.stdin)["name"])')
          SHA=$(printf '%s' "$INFO" | python3 -c 'import json,sys; print(json.load(sys.stdin)["sha256"])')
          curl -sSL -o "journey-server/$NAME" "$URL"
          echo "${SHA}  journey-server/${NAME}" | sha256sum -c -
          ls -la journey-server/
      - name: Assemble server + deterministic overlays
        run: |
          set -euo pipefail
          mkdir -p journey-server/plugins
          JAR=$(ls sandbox/plugin/CoreMC-[0-9]*.jar | head -1)
          echo "plugin under test: $JAR ($(stat -c%s "$JAR") bytes)"
          cp "$JAR" journey-server/plugins/
          printf 'eula=true\n' > journey-server/eula.txt
          cat > journey-server/server.properties <<'EOF'
          server-port=25565
          online-mode=false
          enable-rcon=true
          rcon.port=25575
          rcon.password=journey123
          difficulty=normal
          spawn-monsters=true
          max-tick-time=-1
          view-distance=5
          simulation-distance=5
          level-name=world
          white-list=false
          motd=CoreMC journey
          EOF
          npm --prefix journey install --no-audit --no-fund
          node journey/make-overlays.mjs
          ls -la journey-server/plugins/CoreMC/
      - name: Run live player journey
        working-directory: journey-server
        run: node ../journey/run.mjs
        timeout-minutes: 50
      - name: Upload journey evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: journey-evidence
          path: |
            journey-server/server.log
            journey-server/journey.log
            journey-server/plugins/CoreMC/
      - name: Commit journey logs to sandbox (artifact/log APIs unreachable from sandbox)
        if: always()
        run: |
          git config user.name "arena-journey"
          git config user.email "bot@users.noreply.github.com"
          mkdir -p sandbox/journey
          cp journey-server/journey.log sandbox/journey/journey.log 2>/dev/null \
            || echo "NO journey.log produced" > sandbox/journey/journey.log
          tail -c 400000 journey-server/server.log > sandbox/journey/server-tail.log 2>/dev/null || true
          ls -laR journey-server/plugins/CoreMC/ > sandbox/journey/data-files.txt 2>/dev/null || true
          git add -f sandbox/journey/
          git commit -m "journey evidence: run $GITHUB_RUN_ID" || true
          for i in 1 2 3 4 5; do
            git pull --rebase origin "${GITHUB_REF_NAME}" || true
            if git push origin "HEAD:${GITHUB_REF_NAME}"; then break; fi
            sleep 10
          done
          true
